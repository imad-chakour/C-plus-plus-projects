#include<stdio.h>
#include<stdlib.h>

typedef struct{
    int num;
    int denum;
}RAT;



void convertion(RAT R);

void affichage(RAT R);

RAT saisir(RAT R);

void invertion(RAT R);

void PGCD(RAT R);